<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="nav w-full flex justify-between items-center">
            <h3 class="text-3xl">Hello, <%= post.user.name%> </h3>
            <a class="bg-red-500 rounded-lg px-5 py-2" href="/logout">Logout</a>
        </div>
        <hr class="my-5">
        <h5 class="mb-5">Edit Post</h5>
        <form method="post" action="/update/<%= post._id%>">
            <textarea placeholder="Write Here..." class=" block w-1/2 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content">
                <%= post.content%>
            </textarea>
            <input class="bg-yellow-500 rounded-lg text-black px-5 py-2 block mt-5" type="submit" value="Update Post">
        </form>
    </div>
</body>
</html>