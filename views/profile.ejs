<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="nav w-full flex justify-between items-center">
            <h3 class="text-3xl">Hello, <%= user.name%> </h3>
            <a class="bg-red-500 rounded-lg px-5 py-2" href="/logout">Logout</a>
        </div>
        <hr class="my-5">
        <h5 class="mb-5">Create Post</h5>
        <form method="post" action="/createpost">
            <textarea placeholder="Write Here..." class=" block w-1/2 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
            <input class="bg-blue-500 rounded-lg px-5 py-2 block mt-5" type="submit" value="Create Post">
        </form>
        <hr class="my-5">
        <div class="posts">
            <h3 class="text-2xl text-zinc-400">Yours Post</h3>
            <div class="postconatiner mt-5 flex flex-wrap gap-4">
                <% user.posts.reverse().forEach(function(e){ %>
                    <div class="post w-1/3 p-4 rounded-md bg-zinc border-2 border-zinc-700 bg-zinc-800">
                        <h4 class="text-blue-500 mb-2">@<%= user.username%></h4>
                        <p class="text-sm tracking-tight"><%= e.content%></p>
                        <div class="btns flex justify-between mt-5">
                            <a class="text-blue-500" href="/like/<%= e._id %>">
                                <%= e.likes.indexOf(user._id) === -1 ? "Like" : "Unlike"%>
                                <small><%= e.likes.length %></small></a>
                            <a class="text-zinc-600" href="/edit/<%= e._id%>">Edit</a>
                        </div>
                    </div>
                <%})%>


               
            </div>

        </div>
    </div>
</body>
</html>